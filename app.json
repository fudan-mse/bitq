{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "cd webapp && pip install -r requirements.txt && cd ..",
        "test": "cd webapp && python tests.py && cd .."
      }
    }
  }
}
